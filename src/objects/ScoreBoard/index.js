import { PlayerName } from "../../components";

import "./style.css";

export function ScoreBoard() {
  return; /*html*/
  ` <header>
      ${PlayerName("Player 1")} vs ${PlayerName("Player 2")}
    </header>
  `;
}
